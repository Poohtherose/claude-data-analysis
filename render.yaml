services:
  - type: web
    name: mhy-shower-analysis
    runtime: python
    buildCommand: bash build.sh
    startCommand: gunicorn app:app --workers 4 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
